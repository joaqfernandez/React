import React, {useState, useEffect} from 'react';

export default function ItemListContainerLayout({productos}) {
    return (
        <div>
            {JSON.stringify(productos)}
        </div>
    )
}
